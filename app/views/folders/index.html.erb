<button type="button" class="btn btn-secondary"><%= link_to "新規投稿ページへ", new_folder_path %></button>

<div class="folders-container">
  <div class="row">
   
      <% @folders.each do |t| %>
        <div class="col-sm-4 box1">
          <div class="folder">
            <div class="card text-white mb-3" style="max-width: 18rem;">
            <div class="card-body">
              <h5 class="card-title"><%= t.folder %></h5>
              <%= link_to "詳細", folder_path(t.id),class: 'btn' %>
            </div>
            </div>
          </div>
        </div>
      <% end %>
  </div>
</div>

<div class="sheets-container">
  <div class="row">

  <% @sheets.each do |t| %>
    <div class="col-sm-4 box1">
      <div class="sheet">
    
      <div class="card text-white #3a3e41 mb-3" style="max-width: 18rem;">
            <div class="card-body">
              <h5 class="card-title"><%= t.title %></h5>
              <%= t.body %>
              <%= image_tag t.image_url, size: "250x200" if t.image? %></h5>
            </div>
          </div>
      </div>
    </div>  
  <% end %>
</div>

